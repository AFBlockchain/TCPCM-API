title: QuizRecord
description: |
  Graded record for a quiz. A record can only be graded by the course `chiefProfessor`.
type: object
properties:
  id:
    type: integer
    format: int64
  quizId:
    type: string
  student:
    $ref: ./Student.yaml
  grade:
    type: string
  