title: QuizRecord
description: |
  Graded record for a quiz. A record can only be graded by the course `chiefProfessor`.
type: object
properties:
  ID:
    type: string
    description: This ID is in `UUID` format and is for system book-keeping and not exposed to the end user.
  quizID:
    type: string
  student:
    $ref: ./Student.yaml
  grade:
    type: string
  